#include <stdio.h>
#include <string.h>

/**
	* main - print the message with printf
	* Return: 0 if exited correctly, non-zero if catch
	*/
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
